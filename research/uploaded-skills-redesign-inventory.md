# Uploaded skills inventory for the authenticated redesign

## Available project-local skills

| Skill | Applied contribution | Design consequence |
|---|---|---|
| `STUDIO Frontend Design` | Names a route’s job and committed action before layout; preserves real source, composition, Brand, caption, approval, export, and delivery work; rejects generic dashboards. | Each route receives one task-first composition rather than a shared stack of cards. |
| `STUDIO UI/UX System` | Uses ink, paper, sage, and lime semantically; requires all states, real empty states, mobile re-sequencing, visible focus, and short interaction feedback. | The new system uses a shared command strip and route-specific work surfaces with explicit loading, error, and unavailable states. |
| `tmp_emilkowalski` | Not actionable. The repository retains only an unresolved placeholder with no trusted source or revision. | It is not inferred or imported; no design decision relies on it. |

## Complementary implementation guidance applied

The global frontend-design, UI Designer, Frontend Developer, and UX Architect guidance is applied as supporting implementation direction: first establish tokens and reusable primitives, avoid default dashboard compositions, prioritize direct operational language, use real controls, preserve accessible keyboard paths, respect reduced motion, and validate desktop plus narrow layouts.

## Scope guarantee

The public landing remains unchanged. The redesign covers member routes and the separately governed Admin control room. Existing API contracts, local SVG composition, product image slots, optional Brand mark, four caption fields, approval/export, social connection states, scheduling, and honest unavailable messages remain the source of truth.
