"use client";

import Link from "next/link";
import { motion } from "motion/react";
import { BarChart3, Bell, CalendarDays, ChevronRight, CircleHelp, FileImage, FolderOpen, Layers3, LayoutDashboard, Package, Palette, Plus, Search, Settings2, ShieldCheck, Sparkles, Store, Users2, ArrowRight, ArrowUpRight } from "lucide-react";
import { StudioMark } from "./StudioShell";

const workspaceData = {
  dashboard: { label: "Overview", title: "Your creative operating system", description: "A live read on what is moving, waiting, and ready to ship.", icon: LayoutDashboard },
  products: { label: "Products", title: "Keep the source material close", description: "Organize product references, campaign inputs, and approved visual directions.", icon: Package },
  brand: { label: "Brand kit", title: "Make consistency feel expressive", description: "Your colors, voice, and rules—available before the next draft begins.", icon: Palette },
  studio: { label: "Studio", title: "Compose the next direction", description: "Bring a prompt, a product, or an unfinished thought. Leave with a visual thread.", icon: Sparkles },
  batch: { label: "Batch", title: "More outputs, same point of view", description: "Create a considered family of assets without starting from scratch each time.", icon: Layers3 },
  assets: { label: "Assets", title: "A library with memory", description: "Find the approved crop, caption, and campaign state in fewer clicks.", icon: FolderOpen },
  posts: { label: "Posts", title: "Keep the publishing thread intact", description: "Move from draft to approval with the original creative context attached.", icon: FileImage },
  calendar: { label: "Calendar", title: "See the next move", description: "Map campaigns, channels, and handoffs before they become urgent.", icon: CalendarDays },
  social: { label: "Social", title: "Publish with intent", description: "Bring channel constraints into the same creative conversation.", icon: Store },
  notifications: { label: "Notifications", title: "Signals, not noise", description: "Stay close to the moments that need your point of view.", icon: Bell },
  settings: { label: "Settings", title: "Shape your workspace", description: "Tune permissions, defaults, integrations, and the way your team moves.", icon: Settings2 },
} as const;

const workspaceNav = [["dashboard", "Overview", LayoutDashboard], ["products", "Products", Package], ["brand", "Brand", Palette], ["studio", "Studio", Sparkles], ["batch", "Batch", Layers3], ["assets", "Assets", FolderOpen], ["posts", "Posts", FileImage], ["calendar", "Calendar", CalendarDays], ["social", "Social", Store], ["notifications", "Notifications", Bell], ["settings", "Settings", Settings2]] as const;

function WorkspaceSidebar({ active }: { active: string }) {
  return <aside className="studio-workspace-sidebar"><div className="studio-workspace-sidebar__brand"><StudioMark compact /><span>WORKSPACE / 01</span></div><nav>{workspaceNav.map(([key, label, Icon]) => <Link key={key} href={`/dashboard/${key}`} className={active === key ? "is-active" : ""}><Icon size={16} />{label}</Link>)}</nav><div className="studio-workspace-sidebar__bottom"><Link href="/contact"><CircleHelp size={15} />Need a hand?</Link><Link href="/">← Back to site</Link></div></aside>;
}

export function WorkspacePage({ mode }: { mode: keyof typeof workspaceData }) {
  const data = workspaceData[mode];
  const Icon = data.icon;
  return <main className="studio-app"><WorkspaceSidebar active={mode} /><div className="studio-workspace-main"><header className="studio-workspace-topbar"><div><span className="studio-kicker studio-kicker--dark">WORKSPACE / {data.label.toUpperCase()}</span><h1>{data.title}</h1><p>{data.description}</p></div><div className="studio-workspace-actions"><button className="studio-icon-button" aria-label="Search"><Search size={17} /></button><Link href="/dashboard/studio" className="studio-button studio-button--dark"><Plus size={15} /> New direction</Link></div></header><section className="studio-workspace-content"><div className="studio-command-row"><div className="studio-route-title"><Icon size={20} /><span>{data.label}</span></div><label className="studio-search"><Search size={15} /><input placeholder={`Search ${data.label.toLowerCase()}`} /></label></div><div className="studio-stat-grid"><div><span>ACTIVE THREADS</span><strong>08</strong><small>+2 this week</small></div><div><span>READY TO SHIP</span><strong>24</strong><small>Across 4 channels</small></div><div><span>TEAM SIGNAL</span><strong>92%</strong><small>On brief this cycle</small></div><div><span>LAST MOVEMENT</span><strong>14m</strong><small>Caption approved</small></div></div><div className="studio-workspace-grid"><div className="studio-workspace-panel studio-workspace-panel--wide"><div className="studio-panel-heading"><div><span className="studio-kicker studio-kicker--dark">LIVE BOARD</span><h2>{mode === "studio" ? "Creative directions in motion" : mode === "calendar" ? "This week in the system" : "Recent activity"}</h2></div><Link href="/dashboard/studio">View all <ArrowRight size={14} /></Link></div><div className="studio-activity-list">{["Summer 26 / campaign visual", "Product story / caption family", "Retail launch / social cutdowns", "Brand kit / updated voice notes"].map((item, index) => <motion.div key={item} className="studio-activity-row" initial={{ opacity: 0, x: -8 }} animate={{ opacity: 1, x: 0 }} transition={{ delay: index * .06 }}><span className={`studio-activity-status ${index === 0 ? "is-live" : ""}`} /><div><strong>{item}</strong><small>{index === 0 ? "Model running" : index === 1 ? "Awaiting review" : "Draft direction"}</small></div><span className="studio-activity-meta">0{index + 1} / 04</span><ChevronRight size={15} /></motion.div>)}</div></div><div className="studio-workspace-panel studio-workspace-panel--accent"><span className="studio-kicker studio-kicker--dark">NEXT MOVE</span><h2>{mode === "settings" ? "Make the system yours." : "Turn one brief into more."}</h2><p>{mode === "settings" ? "Set the defaults that keep your team moving without asking the same question twice." : "The best creative teams keep the original thought visible as the output expands."}</p><Link href={mode === "settings" ? "/dashboard/brand" : "/dashboard/studio"} className="studio-button studio-button--dark">Open canvas <ArrowUpRight size={15} /></Link></div></div><div className="studio-workspace-panel studio-workspace-panel--table"><div className="studio-panel-heading"><div><span className="studio-kicker studio-kicker--dark">RECENT FILES</span><h2>Working memory</h2></div><button className="studio-text-button">Sort by recent <ChevronRight size={14} /></button></div><div className="studio-file-grid">{["campaign_direction_04", "caption_family_en-fr-ar", "model_crop_02", "social_cutdown_square"].map((file, index) => <div key={file} className="studio-file-card"><div className={`studio-file-thumb studio-file-thumb--${index + 1}`} /><strong>{file}</strong><small>{index % 2 ? "Caption system" : "Visual direction"} · 2h ago</small></div>)}</div></div></section></div></main>;
}

const adminData = {
  dashboard: { label: "Admin overview", title: "Keep the system healthy", icon: ShieldCheck }, users: { label: "Users", title: "The people moving the work", icon: Users2 }, workspaces: { label: "Workspaces", title: "Where the work lives", icon: Layers3 }, products: { label: "Products", title: "Source material review", icon: Package }, content: { label: "Content", title: "Editorial control room", icon: FileImage }, templates: { label: "Templates", title: "Reusable creative scaffolds", icon: Sparkles }, generations: { label: "Generations", title: "Model activity", icon: BarChart3 }, publishing: { label: "Publishing", title: "What is moving out", icon: ArrowUpRight }, analytics: { label: "Analytics", title: "Read the motion", icon: BarChart3 }, "audit-logs": { label: "Audit logs", title: "A clear paper trail", icon: CircleHelp }, settings: { label: "Admin settings", title: "System controls", icon: Settings2 },
} as const;

export function AdminPage({ mode }: { mode: keyof typeof adminData }) {
  const data = adminData[mode];
  const Icon = data.icon;
  return <main className="studio-app studio-admin"><aside className="studio-workspace-sidebar"><div className="studio-workspace-sidebar__brand"><StudioMark compact /><span>ADMIN / CONTROL ROOM</span></div><nav>{Object.entries(adminData).map(([key, item]) => <Link key={key} href={`/admin/${key}`} className={mode === key ? "is-active" : ""}><item.icon size={16} />{item.label}</Link>)}</nav><div className="studio-workspace-sidebar__bottom"><Link href="/dashboard"><LayoutDashboard size={15} />Workspace</Link><Link href="/">← Back to site</Link></div></aside><div className="studio-workspace-main"><header className="studio-workspace-topbar"><div><span className="studio-kicker studio-kicker--dark">CONTROL ROOM / {data.label.toUpperCase()}</span><h1>{data.title}</h1><p>Operations, governance, and the signals behind the creative system.</p></div><div className="studio-admin-status"><span className="studio-dot studio-dot--lime" /> All systems nominal</div></header><section className="studio-workspace-content"><div className="studio-command-row"><div className="studio-route-title"><Icon size={20} /><span>{data.label}</span></div><span className="studio-chip">LAST SYNC / 04m AGO</span></div><div className="studio-stat-grid studio-stat-grid--admin"><div><span>ACTIVE USERS</span><strong>184</strong><small>+14 this month</small></div><div><span>WORKSPACES</span><strong>26</strong><small>3 need review</small></div><div><span>GENERATIONS</span><strong>1.8k</strong><small>98% completed</small></div><div><span>QUEUE HEALTH</span><strong>99.2%</strong><small>Within threshold</small></div></div><div className="studio-admin-board"><div className="studio-workspace-panel studio-workspace-panel--wide"><div className="studio-panel-heading"><div><span className="studio-kicker studio-kicker--dark">OPERATIONS FEED</span><h2>{mode === "analytics" ? "Signal over time" : mode === "audit-logs" ? "Recent audit activity" : "Latest movement"}</h2></div><button className="studio-text-button">Export view <ArrowUpRight size={14} /></button></div><div className="studio-audit-list">{["Workspace / Northstar added a new brand kit", "Generation / model-run-004 completed in 18s", "Publishing / campaign_04 approved by Aisha", "User / rami@studio.co joined the team", "Template / social-cutdowns updated"].map((item, index) => <div className="studio-audit-row" key={item}><span className="studio-audit-time">{index + 1}m</span><span className="studio-dot studio-dot--lime" /><div><strong>{item}</strong><small>{index % 2 ? "Automated event" : "Workspace activity"}</small></div><ChevronRight size={14} /></div>)}</div></div><div className="studio-workspace-panel studio-workspace-panel--dark"><span className="studio-kicker">ADMIN NOTE</span><h2>Keep the edges clear.</h2><p>Good operations make more room for creative judgement. Review what changed, then get out of the way.</p><Link href="/admin/settings" className="studio-button studio-button--lime">Open settings <ArrowUpRight size={15} /></Link></div></div></section></div></main>;
}
