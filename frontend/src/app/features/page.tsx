/* STUDIO feature rule: the Features route is an interactive scroll narrative, not a static marketing grid. */
import FeaturesInteractive from "@/components/studio/FeaturesInteractive";

export default function FeaturesPage() {
  return <FeaturesInteractive />;
}
