import { UtilityPage } from "@/components/studio/StudioShell";
export default function ResetPasswordPage() { return <UtilityPage title="A fresh start for the workspace." body="Choose a new password, then get back to the next draft." action="Set new password" />; }
