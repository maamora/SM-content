// Stub for static asset imports (CSS, images, SVGs) in Jest
// Without this, importing these files in component tests throws a SyntaxError.
const fileMock = "test-file-stub";
export default fileMock;
module.exports = fileMock;
