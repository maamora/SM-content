# Test-only spring-dotenv configuration. Keep developer-local backend/.env files
# out of the test ApplicationContext; individual tests supply their own properties.
directory=/tmp/studio-test-empty-env
ignoreIfMissing=true
